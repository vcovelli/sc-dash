# Database Configuration
APP_DB_NAME=supplywise_db
APP_DB_USER=postgres
APP_DB_PASSWORD=postgres123

# Airflow Database
AIRFLOW_DB_NAME=airflow_db
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=airflow123

# MinIO Object Storage Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# Local Development MinIO Settings (use these for docker-compose)
MINIO_ENDPOINT=minio:9000
MINIO_HTTP_ENDPOINT=http://minio:9000
MINIO_SECURE=false
MINIO_BUCKET_NAME=uploads

# Production MinIO Settings (uncomment and modify for production)
# MINIO_ENDPOINT=minio.supplywise.ai
# MINIO_HTTP_ENDPOINT=https://minio.supplywise.ai
# MINIO_SECURE=true
# MINIO_BUCKET_NAME=production-uploads

# Airflow Configuration
AIRFLOW_USERNAME=airflow
AIRFLOW_PASSWORD=airflow
AIRFLOW_API_BASE=http://airflow-webserver:8080/api/v1

# Other Configuration
SCHEMA_DIR=./user_schemas